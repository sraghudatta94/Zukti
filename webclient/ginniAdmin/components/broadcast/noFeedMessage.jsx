import React from 'react';
import { Message } from 'semantic-ui-react';
const noFeedMessage = () => (
  <Message info>
    <Message.Header>Welcome admin</Message.Header>
    <p>You can broadcast messages to bot users</p>
  </Message>
);

export default noFeedMessage;
